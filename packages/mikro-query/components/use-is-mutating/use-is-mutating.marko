<attrs/{filters} />

<use-query-client/queryClient />

<let/count=queryClient.isMutating(filters) />

<lifecycle
  onMount() {
    this.disposer = queryClient.on('update', () => {
      count = queryClient.isMutating(filters);
    });
  }

  onDestroy() {
    this.disposer();
  }
/>

<return=count />
